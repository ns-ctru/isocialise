```{r analysis, cache = FALSE, echo = FALSE, warnings = FALSE, results = 'hide', eval = TRUE}
n           <- 20
num_levels  <- 2
block_sizes <- 4
primary_small <- blockrand(n            = n,
                           num.levels   = mnum_levels,
                           levels       = c('LEGO Therapy', 'Standard Care'),
                           id           = '',
                           id.prefix    = 'PRI_SM',
                           stratum      = 'Primary; <= 3',
                           block.sizes  = block_sizes)
primary_large <- blockrand(n          = n,
                           num.levels = mnum_levels,
                           levels     = c('LEGO Therapy', 'Standard Care'),
                           id         = '',
                           id.prefix = 'PRI_LG',
                           stratum    = 'Primary; > 3',
                           block.sizes = block_sizes)
secondary_small <- blockrand(n          = n,
                             num.levels = mnum_levels,
                             levels     = c('LEGO Therapy', 'Standard Care'),
                             id         = '',
                             id.prefix = 'SEC_SM',
                             stratum    = 'Secondary; <= 3',
                             block.sizes = block_sizes)
secondary_large <- blockrand(n          = n,
                             num.levels = mnum_levels,
                             levels     = c('LEGO Therapy', 'Standard Care'),
                             id         = '',
                             id.prefix = 'SEC_LG',
                             stratum    = 'Secondary; > 3',
                             block.sizes = block_sizes)
randomisation <- rbind(primary_small,
                       primary_large,
                       secondary_small,
                       secondary_large)

```
